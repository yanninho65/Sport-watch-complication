<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_score_complication"
        android:label="@string/app_name">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!--
             Fournit les données de score aux complications (LONG_TEXT et
             SMALL_IMAGE). UPDATE_PERIOD_SECONDS=60 : le système rappelle
             la complication ~chaque minute pour recalculer la minute de
             jeu estimée (MatchClock), en plus du rafraîchissement
             immédiat déclenché manuellement dès qu'une nouvelle donnée
             arrive du téléphone (ComplicationDataSourceUpdateRequester).
        -->
        <service
            android:name=".ScoreComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_score_complication"
            android:label="@string/complication_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="LONG_TEXT,SMALL_IMAGE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="60" />
            <meta-data
                android:name="android.support.wearable.complications.PROVIDER_CONFIG_ACTION"
                android:value="com.yann.sportscomplication.CONFIG_TEAM_SIDE" />
        </service>

        <!--
             Affiché par le système quand l'utilisateur assigne "Score en
             direct" à un cercle du Dashboard : choix domicile/extérieur
             pour CET emplacement précis.
        -->
        <activity
            android:name=".TeamSideConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.yann.sportscomplication.CONFIG_TEAM_SIDE" />
                <category android:name="android.support.wearable.complications.category.PROVIDER_CONFIG" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
             Reçoit les mises à jour de match envoyées par l'app téléphone
             via la Data Layer API (chemin "/match").
        -->
        <service
            android:name=".MatchListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/match" />
            </intent-filter>
        </service>

    </application>

</manifest>
